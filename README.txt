Notes
1. An order also stores its own shipping address, since customers might ship to a new address that isn’t saved in their profile.

2. Inventory only tracks the global stock — each product has a single inventory record.